apiVersion: v1
kind: Service
metadata:
  name: qa-sentinel
  namespace: qa-sentinel
  labels:
    app: qa-sentinel
spec:
  selector:
    app: qa-sentinel
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
